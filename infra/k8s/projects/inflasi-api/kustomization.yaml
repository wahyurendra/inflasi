apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# secret.example.yaml is intentionally NOT included — the real `inflasi-secret`
# is created manually in-cluster (see k3s-manifest/README).
resources:
  - deployment.yaml
  - service.yaml
  - cronjobs.yaml
