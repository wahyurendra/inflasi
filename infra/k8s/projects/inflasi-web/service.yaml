apiVersion: v1
kind: Service
metadata:
  name: inflasi-web
  namespace: default
  labels: { app: inflasi-web }
spec:
  type: ClusterIP
  selector: { app: inflasi-web }
  ports:
    - { name: http, port: 80, targetPort: 3000 }
