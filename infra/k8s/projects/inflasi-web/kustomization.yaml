apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# secret.example.yaml NOT included — real inflasi-web-secret created manually.
resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - middleware.yaml
