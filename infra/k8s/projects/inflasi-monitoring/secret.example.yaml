# Example only. Create in-cluster:
#   kubectl create secret generic inflasi-grafana-secret -n default \
#     --from-literal=admin-password='<strong-password>'
apiVersion: v1
kind: Secret
metadata:
  name: inflasi-grafana-secret
  namespace: default
type: Opaque
stringData:
  admin-password: "change-me"
