# CPU training deps. TFT additionally needs torch / pytorch-forecasting /
# lightning — those are installed only in the GPU image (Dockerfile.gpu).
numpy>=1.26
pandas>=2.2
pyarrow>=14
scikit-learn>=1.4
statsmodels>=0.14
prophet>=1.1
lightgbm>=4.0
joblib>=1.3
minio>=7.2
httpx>=0.27
